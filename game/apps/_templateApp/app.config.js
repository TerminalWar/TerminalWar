export const TEMPLATE_APP_LOCAL_CONFIG = {
  cloneInstructions: "Copy this folder, rename the slug in game/config/apps.config.js, then replace only the unique UI/logic files.",
  balance: {
    cost: 0,
    cooldownSeconds: 0,
    successChance: 1
  },
  featureFlags: {
    enabledInStartMenu: true,
    enabledOnDesktop: true,
    requiresNetwork: false
  }
};
